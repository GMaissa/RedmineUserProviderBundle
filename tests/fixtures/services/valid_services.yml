gm_redmine_user_provider:
    redmine:
        url: redmine.test.com
        allowed_domains:
            - test.com
    user_repository_service: "gm_redmine_user_provider.repository.user.custom"

services:
    gm_redmine_user_provider.repository.user.custom:
        class: \GMaissa\RedmineUserProviderBundle\Tests\Mock\Repository\ValidCustomUserRepository
